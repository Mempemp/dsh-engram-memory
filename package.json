{
  "name": "dsh-engram-memory",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "main": "lib/index.js",
  "license": "MIT",
  "description": "Engram memory for DSH: guidance in the system prompt, pre-step recall, automatic capture — self-contained, with the engram MCP server inside the package",
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  },
  "files": [
    "lib",
    "mcp",
    "docs",
    "cordis.patch.yml",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "test": "node test/store.smoke.mjs && node test/capture.smoke.mjs && node test/hook.smoke.mjs && node test/mcp-setup.smoke.mjs"
  }
}

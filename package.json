{
  "name": "dsh-engram-memory",
  "version": "1.5.2",
  "private": true,
  "type": "module",
  "main": "lib/index.js",
  "exports": {
    ".": "./lib/index.js",
    "./client": "./lib/client.js",
    "./package.json": "./package.json"
  },
  "license": "MIT",
  "description": "Engram memory for DSH: guidance in the system prompt, pre-step recall, automatic capture, a settings tab to process raw notes — self-contained, with the engram MCP server inside the package",
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    },
    "client": {
      "inject": [
        "@deepseek-ai/dsh-client-runtime",
        "@deepseek-ai/dsh-client-ui-slots"
      ],
      "platform": "web"
    }
  },
  "peerDependencies": {
    "@deepseek-ai/cordis": "^4.0.1",
    "cordis": "^4.0.0-rc.7",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "files": [
    "lib",
    "mcp",
    "docs",
    "cordis.patch.yml",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "test": "node test/store.smoke.mjs && node test/capture.smoke.mjs && node test/hook.smoke.mjs && node test/mcp-setup.smoke.mjs && node test/consolidate.smoke.mjs && node test/panel.smoke.mjs && node test/client.smoke.mjs && node test/hygiene.smoke.mjs"
  }
}
